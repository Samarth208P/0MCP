/**
 * Context engine — keyword-based retrieval and injection from 0G storage.
 * Deterministic scoring: no embeddings, no external API, zero cost.
 * @module context
 */

// STUB — implemented in Phase 3
export async function buildContext(
  _project_id: string,
  _prompt: string,
  _maxEntries = 5
): Promise<string> {
  throw new Error("context: not implemented yet — Phase 3");
}
